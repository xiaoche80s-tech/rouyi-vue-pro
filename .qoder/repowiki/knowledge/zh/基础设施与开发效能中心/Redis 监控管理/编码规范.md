- 控制器方法统一返回 `CommonResult<T>` 包装类，并通过静态方法 `success()` 构建成功响应。
- VO 类采用 Lombok 的 `@Builder` 和 `@AllArgsConstructor` 注解以支持链式调用和全参构造。
- 权限控制使用 `@PreAuthorize` 注解配合 SpEL 表达式（如 `@ss.hasPermission(...)`）进行细粒度鉴权。