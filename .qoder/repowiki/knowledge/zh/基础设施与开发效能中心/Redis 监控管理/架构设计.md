- 控制层：`RedisController` 暴露 `/infra/redis/get-monitor-info` 接口，通过 `StringRedisTemplate` 执行底层回调获取原生 Redis 数据。
- 转换层：`RedisConvert` 负责将 Redis 原生的 `Properties` 格式（特别是 commandstats）解析并组装为前端友好的 VO 对象。
- 视图层：`RedisMonitorRespVO` 定义监控数据结构，包含基础 info、dbSize 及解析后的命令统计列表。