- API 接口提供 sendObject 默认方法，内部通过 JsonUtils.toJsonString 统一序列化消息内容。
- WebSocket 相关 Bean 注入均标注 @Autowired(required = false)，以支持功能可配置关闭。
- 消息监听器实现 WebSocketMessageListener<T> 泛型接口，并通过 getType() 返回字符串类型标识进行路由。