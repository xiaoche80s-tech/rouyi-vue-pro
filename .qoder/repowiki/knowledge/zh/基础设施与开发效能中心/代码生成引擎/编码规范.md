- 采用后缀匹配策略自动推断字段的 UI 组件类型（如 name -> Input, status -> Radio, time -> DateTime）与列表查询条件（如 name -> LIKE, time -> BETWEEN）。
- 生成的文件路径采用动态占位符替换机制（如 ${table.moduleName}, ${sceneEnum.prefixClass}），根据表定义与场景枚举自动计算目标工程的相对路径。
- 在代码生成后执行统一的格式化清理逻辑，包括去除 Vue 模板中多余的逗号、未使用的字典引用以及修正 Vue2 的 $refs 语法。
- 针对主子表与树形表等特殊业务场景，通过特定的模板命名约定（如 _sub, _normal, _erp）与逻辑判断实现差异化代码生成。