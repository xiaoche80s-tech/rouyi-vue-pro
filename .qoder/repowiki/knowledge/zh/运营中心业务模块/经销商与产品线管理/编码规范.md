- 控制器统一使用`@PreAuthorize`注解配合权限标识（如`dealer:mgmt:create`）进行细粒度的接口访问控制。
- 服务实现类中采用私有校验方法（如`validateDealerCodeUnique`）封装业务规则校验，并在增改操作中强制调用以确保数据一致性。
- 数据对象（DO）统一继承`TenantBaseDO`以自动支持多租户字段注入，并使用Lombok简化样板代码。
- 关联关系表设计摒弃物理外键与自增ID依赖，转而使用具有业务语义的唯一编码（Code）作为关联键，提升数据迁移与同步的灵活性。