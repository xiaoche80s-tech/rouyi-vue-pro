- 所有子域控制器均位于 `controller/admin/{domain}` 路径下，并配套独立的 VO 目录管理请求/响应对象。
- 数据访问层采用 MyBatis-Plus 规范，Mapper 接口统一继承 BaseMapper 并置于 `dal/mysql/{domain}` 包中。
- 业务枚举类统一定义在 `enums` 根包下，采用常量命名规范（如 ContractStatusEnum）以支撑全模块状态流转。