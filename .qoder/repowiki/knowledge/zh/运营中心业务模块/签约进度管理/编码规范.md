- VO 与 DO 之间通过 `BeanUtils.toBean` 进行属性拷贝，保持层级解耦。
- Mapper 层继承 `BaseMapperX` 并使用 `LambdaQueryWrapperX` 构建动态查询条件，支持关键词模糊搜索及时间维度过滤。
- 服务层在执行业务操作前，统一校验关联实体（如经销商）的存在性及当前状态的合法性。
- 合同编码采用“类型前缀-年份-三位序号”的格式，并通过查询最大序号实现并发下的唯一性生成。