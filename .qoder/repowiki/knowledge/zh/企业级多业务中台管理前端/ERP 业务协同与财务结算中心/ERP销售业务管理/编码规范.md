- 所有列表页均遵循“搜索栏 + 表格 + 分页”的标准布局，并使用 `ContentWrap` 包裹内容区域。
- 表单弹窗统一通过 `defineExpose({ open })` 暴露 `open` 方法，由父组件调用以触发弹窗显示和数据初始化。
- 金额与数量字段在表格列和输入框中统一使用 `erpPriceInputFormatter` 和 `erpCountInputFormatter` 进行格式化展示。
- 主子表表单校验时，父表单通过 `ref` 调用子组件暴露的 `validate()` 方法，确保明细数据的有效性。