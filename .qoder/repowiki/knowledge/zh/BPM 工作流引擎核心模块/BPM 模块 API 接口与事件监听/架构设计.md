- 采用接口与实现分离模式：`BpmProcessInstanceApi`/`BpmProcessTaskApi` 定义契约，`*Impl` 类通过 `@Service` 注册并委托给内部 `service.task` 层。
- 事件驱动架构：定义 `BpmProcessInstanceStatusEvent` 继承自 Spring `ApplicationEvent`，并提供抽象监听器基类 `BpmProcessInstanceStatusEventListener`，通过模板方法模式过滤特定流程定义 Key。
- 包结构清晰划分为 `task`（RPC/API 接口）和 `event`（本地事件），实现模块间解耦。