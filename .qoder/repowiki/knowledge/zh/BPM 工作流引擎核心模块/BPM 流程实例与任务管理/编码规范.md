- Controller 层在返回分页或列表数据前，通常先查询基础流程/任务数据，若为空则直接返回空结果，避免不必要的关联数据查询。
- Service 实现类中广泛使用 `@DataPermission(enable = false)` 注解关闭数据权限拦截，确保在流程跨部门/跨用户场景下能正确查询到所有相关数据。
- 流程变量（Process Variables）的更新遵循“历史变量 + 前端传入”的合并策略，确保在表单字段动态变化时流程上下文数据的完整性。