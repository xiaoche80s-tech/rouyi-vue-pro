- 采用基于角色的视图范围（ViewScope）过滤机制，在 Service 层解析用户角色并注入 Mapper 查询条件，实现数据权限隔离。
- 工单编号生成采用“日期前缀+序列号”模式，并在 Mapper 层使用 `FOR UPDATE` 行锁确保高并发下的唯一性。
- 状态流转采用校验前置模式，在执行操作前通过 `validateStatus` 和 `validateIsAssignee` 等方法确保业务规则一致性。
- BPM 流程交互采用“本地状态更新 + 远程流程驱动”的双写模式，并通过 try-catch 捕获 BPM 异常以防止本地事务回滚。