- **数据权限层**：通过 `OpshubDataPermissionConfiguration` 注册 `DealerDataPermissionRule`，实现基于经销商（dealer_code）和产品线（product_line_code）维度的动态 SQL 过滤，并支持处理人旁路机制。
- **Excel 增强层**：定义 `AbstractMapConvert` 抽象基类统一处理枚举的双向转换，并通过 `OpsExcelSelectFunctions` 配置化注入下拉选项数据源。
- **安全配置层**：利用 `SecurityConfiguration` 扩展框架的 `AuthorizeRequestsCustomizer`，管理模块级的接口访问白名单。