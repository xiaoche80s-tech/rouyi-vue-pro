- 控制器统一使用 `@PreAuthorize` 注解配合权限标识（如 `dealer:mgmt:create`）进行细粒度访问控制。
- 服务实现类中采用私有 `validate` 方法（如 `validateDealerCodeUnique`）在执行增删改操作前进行业务规则校验。
- 数据传输对象（VO）严格区分请求与响应场景，并针对下拉选择等场景提供专用的 `SimpleRespVO` 以优化性能。