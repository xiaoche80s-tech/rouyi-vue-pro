- 枚举类统一采用 `@Getter` 和 `@AllArgsConstructor` 注解，包含 `code` 与 `name` 字段，并提供基于 `code` 的静态 `getByCode` 查找方法。
- 错误码常量遵循 `1_050_XXX_YYY` 的分段编号规则，不同业务子域（如订单、售后、客服）占用不同的千位或百位区间以方便排查。
- 角色标识常量在模块内通过独立接口（`OpsRoleCodeConstants`）维护，避免直接修改系统级角色枚举，并通过 `Set.of` 预定义角色分组。