- 所有子域 Controller 均位于 `controller/admin/{domain}` 路径下，并配套独立的 `vo` 包进行请求/响应模型隔离。
- 所有子域采用标准的三层架构（Controller-Service-Mapper），且 Mapper 接口统一继承自 MyBatis Plus 基类并置于 `dal/mysql/{domain}` 包中。
- 业务枚举类统一收敛于模块根目录的 `enums` 包下，按功能前缀（如 `AfterSale`, `Cs`, `Order`）进行分类管理。