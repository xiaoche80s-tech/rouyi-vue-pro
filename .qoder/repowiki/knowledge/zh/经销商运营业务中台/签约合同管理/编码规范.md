- VO 与 DO 之间通过 `BeanUtils.toBean` 进行属性拷贝，避免手动 setter 赋值。
- Controller 层统一使用 `CommonResult.success()` 包装返回结果，并配合 `@PreAuthorize` 进行细粒度权限控制。
- 业务异常通过 `ServiceExceptionUtil.exception()` 抛出，错误码定义在 `ErrorCodeConstants` 中。
- Mapper 接口继承 `BaseMapperX`，利用 `LambdaQueryWrapperX` 构建类型安全的动态查询条件。