- VO 与 DO 之间使用 `BeanUtils.toBean` 进行对象转换，并在 Controller 层统一处理关联字段（如经销商名称）的填充。
- 合同编码生成采用“前缀-年份-三位序号”格式（如 MC-2024-001），序号通过查询当年最大编码并递增实现。
- 状态流转遵循严格校验：发起签署时校验状态为“未签署”，上传凭证时校验状态并更新为“已签署”且清空子状态。
- Mapper 层使用 `LambdaQueryWrapperX` 构建动态查询条件，并通过 `apply` 方法执行数据库特定的时间函数（如 EXTRACT）进行维度过滤。