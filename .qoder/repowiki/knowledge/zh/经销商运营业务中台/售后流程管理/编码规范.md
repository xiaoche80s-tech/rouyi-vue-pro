- 枚举类统一提供 `getByCode` 静态方法用于根据编码查找枚举实例，并在 Service 层进行中文名称的转换与填充。
- VO 对象与 DO 对象之间通过 `BeanUtils.toBean` 进行基础属性拷贝，复杂关联数据（如进度节点列表）则在 Service 或 Controller 中手动组装。
- Mapper 接口继承 `BaseMapperX` 并使用 `LambdaQueryWrapperX` 构建动态查询条件，支持关键词模糊搜索及多字段动态排序。