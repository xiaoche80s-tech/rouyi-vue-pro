- 工单编号采用“前缀+日期+序列号”格式（如 TASK-20231027-001），并通过数据库行锁（FOR UPDATE）确保并发环境下的唯一性。
- 权限控制采用“角色可见范围 + 数据权限”双重过滤，Service 层根据用户角色（经销商/执行员/管理员）动态注入查询条件。
- 涉及多步状态变更或外部系统交互（如 BPM、WebSocket）的业务方法均标注 @Transactional 以确保数据一致性。
- VO 对象转换统一使用 BeanUtils.toBean，并在 Controller 层完成 DO 到 VO 的映射及关联用户信息的补充。