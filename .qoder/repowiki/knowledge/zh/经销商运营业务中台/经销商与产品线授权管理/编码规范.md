- VO 命名遵循 `{Entity}{Operation}ReqVO/RespVO` 规范，如 `DealerInfoSaveReqVO` 和 `DealerInfoRespVO`。
- Mapper 接口继承 `BaseMapperX` 并使用 `LambdaQueryWrapperX` 构建动态查询条件，实现类型安全的数据库操作。
- Service 层在写操作中执行显式业务校验（如编码唯一性、存在性），并抛出统一的业务异常。
- Controller 层统一使用 `BeanUtils.toBean` 进行 DO 到 VO 的转换，保持表现层与持久层对象解耦。