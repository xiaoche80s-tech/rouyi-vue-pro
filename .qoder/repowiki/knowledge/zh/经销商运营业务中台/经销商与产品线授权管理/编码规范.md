- VO 对象具象化：在返回关联数据时，使用包含 Code 和 Name 的专用 VO（如 `DealerItemVO`, `ProductLineItemVO`）而非简单字符串集合，以减少前端二次查询。
- Service 层 DTO 封装：在跨服务或复杂查询场景中，使用内部 DTO（如 `UserScopeDTO`）封装用户 ID 与授权编码集合，作为 Service 层与 Controller 层之间的数据传输媒介。
- 唯一性校验：在新增或更新核心实体（如经销商）时，Service 实现类中强制执行业务键（如 `dealerCode`）的唯一性校验，防止数据冲突。