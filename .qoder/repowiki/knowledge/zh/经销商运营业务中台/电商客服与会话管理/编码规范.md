- 所有涉及状态变更或跨表操作的服务方法（如创建会话、发送消息）均标注`@Transactional`以确保数据一致性。
- 会话编号采用`CS-yyyyMMdd-NNN`格式，通过查询当日最大序号并在内存中递增生成，保证编号的唯一性与可读性。
- 在发送聊天消息后，同步更新会话表的最后一条消息摘要、时间及消息总数，以优化列表页展示性能。
- 控制器层统一使用`BeanUtils.toBean`将DO对象转换为VO对象返回，保持表现层与持久层对象的解耦。