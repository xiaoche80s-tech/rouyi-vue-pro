- 控制器层统一使用 `BeanUtils.toBean` 进行 DO 到 VO 的转换，并通过私有辅助方法批量填充关联字段（如经销商名称、文件类型描述）。
- 有效期状态（valid/expiring_soon/expired）的计算逻辑在 Service 层定义为静态方法，并在 Controller 和 Mapper 筛选逻辑中保持一致。
- Mapper 层利用 `LambdaQueryWrapperX` 提供动态查询能力，通过 `eqIfPresent` 和 `likeIfPresent` 等方法根据请求参数自动构建 SQL 条件。