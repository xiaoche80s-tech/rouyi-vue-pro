- 采用标准分层架构：Controller (`BasedataFileController`) 处理 HTTP 请求与 VO 转换，Service (`BasedataFileServiceImpl`) 执行业务逻辑，Mapper (`BasedataFileMapper`) 负责数据持久化。
- 依赖注入 `DealerInfoService` 以在响应中动态填充经销商名称，实现跨模块数据关联。
- 集成 `FileApi` 生成文件预签名下载链接，确保存储安全。
- 数据对象 `BasedataFileDO` 继承 `TenantBaseDO`，自动支持多租户隔离。