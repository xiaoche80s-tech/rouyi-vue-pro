- VO 与 DO 分离：Controller 层使用 ReqVO/RespVO 接收和返回数据，Service 层内部转换为 DO 进行持久化操作，利用 BeanUtils 进行对象映射。
- 业务异常标准化：在 Service 实现类中通过静态方法引用错误码常量（如 `DEALER_NOT_EXISTS`）抛出业务异常，确保错误提示的统一性。
- 数据权限上下文缓存：在 `DealerDataPermissionRule` 中将计算后的权限数据（如授权的经销商 Code 集合）缓存至 `LoginUser.context`，避免同一请求内重复查询数据库。