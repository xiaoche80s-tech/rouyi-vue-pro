- 控制器方法统一使用 `@PreAuthorize` 注解配合权限标识（如 `dealer:mgmt:create`）进行接口级访问控制。
- 服务实现类中采用私有校验方法（如 `validateDealerCodeUnique`）处理业务约束，并在违反时抛出基于 `ErrorCodeConstants` 的业务异常。
- 数据传输对象严格区分请求（ReqVO）与响应（RespVO），并通过 `BeanUtils.toBean` 在 DO 与 VO 之间进行转换。