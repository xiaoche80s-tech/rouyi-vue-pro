采用经典的分层架构：
- 接口层：`OrderInfoController` 提供 RESTful API，集成权限校验（`@PreAuthorize`）与参数验证。
- 业务层：`OrderInfoService` 封装核心逻辑，包括状态流转校验、多表聚合查询（详情含6个Tab数据）及批量操作处理。
- 数据层：通过 MyBatis Plus Mapper 访问 `ops_order_info` 及其关联子表（物流、支付、发票、商品、时间轴），DO 对象继承 `TenantBaseDO` 实现多租户隔离。
- 枚举层：定义订单进度、支付、发票等状态枚举，确保业务状态的一致性。