- 枚举类统一提供 `getByCode(String code)` 静态方法用于根据编码查找枚举实例，未匹配时返回 null。
- 数据对象（DO）继承 `TenantBaseDO` 以支持多租户隔离，并使用 MyBatis-Plus 注解（`@TableName`, `@TableId`）进行 ORM 映射。
- 服务层更新操作使用 `@Transactional(rollbackFor = Exception.class)` 确保主表与进度子表数据的一致性。