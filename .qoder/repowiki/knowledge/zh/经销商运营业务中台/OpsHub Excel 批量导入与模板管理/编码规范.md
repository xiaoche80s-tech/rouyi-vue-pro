- 导入逻辑中普遍采用 Map 预加载全量基础数据（如经销商、产品线），以避免在循环中进行数据库查询，提升批量处理性能。
- 所有导入方法均遵循“存在则更新，不存在则新增”的幂等逻辑，并基于业务编码（Code）作为唯一标识进行判断。
- 异常处理采用行级捕获机制，单行数据校验或处理失败不中断整体流程，而是记录行号与错误信息至 failureRows 映射中。
- VO 类与 DO 实体之间的转换通常在 Service 层手动完成，并在插入前进行必要的关联 ID 填充（如根据 DealerCode 获取 DealerId）。