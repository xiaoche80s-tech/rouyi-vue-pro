- 数据权限规则采用角色驱动的维度隔离策略，super_admin 全量可见，brand/service 角色按产品线过滤，dealer 角色按经销商过滤。
- Excel 枚举转换采用抽象基类 `AbstractMapConvert` 统一处理 String 与 Label/Code 的双向映射，子类仅需提供 Map 数据源。
- Excel 下拉选项通过定义 `ExcelColumnSelectFunction` Bean 并指定唯一 `name` 的方式集中管理，供 `@ExcelColumnSelect` 注解引用。