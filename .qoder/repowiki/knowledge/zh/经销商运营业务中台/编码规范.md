- 所有子模块的 Controller 均位于 `controller/admin` 目录下，并严格遵循 VO (Req/Resp) 分离模式。
- 各子模块均采用标准的三层架构（Controller-Service-Mapper），且 Service 接口与实现类分别置于 `service` 与 `service/impl` 包中。
- 业务状态与类型定义统一收敛至父模块的 `enums` 包下，供各子模块共享引用以确保语义一致性。