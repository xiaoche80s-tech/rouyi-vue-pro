- **分层架构**：遵循 Controller-Service-Mapper-DO 经典四层结构，入口为 `BasedataFileController`，业务逻辑在 `BasedataFileServiceImpl`，数据访问通过 `BasedataFileMapper`。
- **依赖方向**：Controller 依赖 Service 接口；Service 实现类依赖 Mapper 及基础设施 API（`FileApi` 用于生成预签名 URL）；Mapper 继承 `BaseMapperX` 并配合 `LambdaQueryWrapperX` 实现动态查询。
- **枚举驱动**：通过 `BasedataCategoryEnum` 和 `BasedataFileTypeEnum` 统一定义文件分类与子类型，确保业务语义一致性。
- **跨模块协作**：在响应组装阶段，Controller 调用 `DealerInfoService` 补全经销商名称，体现模块间的数据关联。