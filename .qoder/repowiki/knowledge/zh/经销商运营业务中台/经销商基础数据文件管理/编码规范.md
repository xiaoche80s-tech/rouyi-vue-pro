- VO 对象中通过枚举类（如 `BasedataFileTypeEnum`）将编码转换为可读名称（如 `fileTypeName`），提升前端展示体验。
- 有效期状态判定采用统一的阈值逻辑（当前日期 + 30天），在 Service 实现类和 Mapper 动态 SQL 中保持一致的判定标准。
- 预签名 URL 生成时固定设置过期时间（如 300 秒），平衡安全性与访问便利性，并通过 `FileApi` 屏蔽具体存储后端差异。