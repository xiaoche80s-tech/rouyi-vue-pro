- API模块采用目录隔离策略，每个业务实体（如csTask）对应独立的index.ts文件，集中导出VO接口与请求函数。
- 视图组件通过Props注入角色标识（如side='handler'），在模板中利用v-if/v-hasPermi实现细粒度的UI权限控制与按钮显隐。
- 复杂表单操作（如转单、退回、提交审批）统一采用Dialog弹窗配合响应式Form对象，并在提交后触发全局getList刷新数据。
- 实时消息处理遵循“事件驱动”模式，WebSocket回调中根据消息类型（chat-message/session-event）分别执行局部消息追加或全局列表重载。