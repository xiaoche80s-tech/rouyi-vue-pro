- 使用 `v-hasPermi` 指令结合权限标识符（如 `dealer:order:pay`）控制批量操作按钮的显示与禁用状态。
- 在表格组件中通过 `show-summary` 属性配合自定义 `summary-method` 实现金额类字段的自动合计展示。
- 采用 `ref` 暴露子组件方法（如 `open`）的模式，由父组件触发模态框的开启与数据加载。
- 统一使用 `formatDate` 和 `Number.toLocaleString` 工具函数处理日期与金额的格式化展示。