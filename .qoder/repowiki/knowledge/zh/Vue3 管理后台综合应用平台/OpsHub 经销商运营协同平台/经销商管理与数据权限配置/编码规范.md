- API 模块按业务域细分目录（如 `dealer`、`dealerScope`），并在 `index.ts` 中统一定义 TypeScript 接口类型与请求方法。
- 列表页与表单组件物理分离，主页面通过 `ref` 引用表单组件实例并调用其 `open` 方法触发弹窗，通过 `@success` 事件实现列表刷新。
- 权限配置类页面采用“先查询用户已有授权范围，再在抽屉中初始化多选框”的模式，确保编辑态数据的一致性。
- 使用 `el-tag` 的 `closable` 属性实现列表项的快速移除，并配合二次确认弹窗（`message.confirm`）防止误操作。