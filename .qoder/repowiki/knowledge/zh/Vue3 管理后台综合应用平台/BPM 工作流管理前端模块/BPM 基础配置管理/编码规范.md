- API 接口定义统一使用 TypeScript 类型约束，并采用 `async/await` 配合 `request` 工具类进行 HTTP 调用。
- 列表页与表单弹窗解耦，表单逻辑封装在独立子组件中，通过 `ref` 调用子组件方法打开弹窗，并通过 `emit('success')` 通知父组件刷新列表。
- 通用状态字段（如 `status`）统一使用 `DICT_TYPE.COMMON_STATUS` 字典进行渲染和选择，保持数据展示一致性。
- 删除操作均包含二次确认机制，使用 `message.delConfirm()` 防止误操作，并在成功后自动刷新列表。