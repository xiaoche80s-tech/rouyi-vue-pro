- 列表组件采用“搜索表单 + 数据表格 + 分页”三段式布局，搜索与表格分别包裹在 `ContentWrap` 中。
- 查询参数使用 `reactive` 对象管理，搜索时重置页码并调用统一 `getList` 方法刷新数据。
- 删除操作先通过 `message.delConfirm()` 进行二次确认，成功后提示并重新加载列表。
- 用户字段通过预加载的 `userList` 在前端映射为昵称显示，避免在表格行中重复请求用户信息。