- 子表单组件通过 `defineExpose` 暴露 `validate` 方法，由父级向导页统一触发校验逻辑。
- 使用 `provide/inject` 机制在向导页与设计组件间共享响应式工作流图谱数据 (`workflowData`)。
- API 模块采用命名导出函数（如 `getWorkflowPage`），并在视图层通过 `import * as WorkflowApi` 方式引用。