- 所有业务模块遵循 `api/`（接口定义）、`views/`（页面组件）、`components/`（局部组件）的分层目录结构。
- 普遍采用 `useXxx` 组合式函数封装业务逻辑（如 `useTable`、`useCrudSchemas`），实现视图层与逻辑层的分离。
- 使用 `defineOptions({ name: 'Xxx' })` 或文件名规范为组件命名，以确保 KeepAlive 缓存与调试信息的准确性。
- API 请求统一通过 `src/api` 下的模块化文件进行管理，并按业务域（如 `system`、`infra`、`bpm`）进行物理隔离。