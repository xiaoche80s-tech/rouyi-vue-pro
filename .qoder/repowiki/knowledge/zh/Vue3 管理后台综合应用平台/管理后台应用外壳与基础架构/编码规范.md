- 组件采用 `src/index.ts` 统一导出模式，支持按需引入与全局注册。
- 样式类名通过 `useDesign` Hook 生成带命名空间的前缀（如 `v-layout`），避免全局污染。
- Store 模块同时导出 `useXxxStore` 组合式函数与 `useXxxStoreWithOut` 非组件环境调用方法。
- 路由元信息（meta）用于控制页面标题、缓存策略及权限标识，由守卫逻辑统一解析。