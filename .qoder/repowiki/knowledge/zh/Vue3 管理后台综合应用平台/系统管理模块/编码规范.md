- API模块统一使用 index.ts 作为默认导出入口，封装具体的HTTP请求方法。
- 视图模块遵循 index.vue（列表页）与 [Entity]Form.vue（表单弹窗）的标准文件命名规范。
- 复杂选择场景统一采用 [Entity]Select.vue 或 [Entity]TreeSelect.vue 组件化封装。
- 详情查看功能统一通过 [Entity]Detail.vue 组件或抽屉形式实现，与编辑表单分离。