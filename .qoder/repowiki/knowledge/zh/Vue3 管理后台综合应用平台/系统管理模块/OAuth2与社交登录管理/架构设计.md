- 采用API层与视图层分离架构，API层定义数据接口与类型（如`client.ts`, `token.ts`, `social/client/index.ts`），视图层负责UI交互与业务逻辑。
- OAuth2模块包含客户端管理（增删改查）与令牌管理（查询与撤销），社交模块包含社交客户端配置与社交用户信息查询。
- 视图层通过组合式API（Composition API）组织逻辑，使用`Dialog`组件封装表单弹窗，通过`emit`事件实现父子组件通信。
- 依赖统一的HTTP请求封装（`@/config/axios`）与字典工具（`@/utils/dict`）进行状态与类型展示。