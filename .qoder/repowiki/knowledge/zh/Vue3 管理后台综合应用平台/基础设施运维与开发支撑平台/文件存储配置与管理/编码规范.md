- API 模块按业务域分包（file/fileConfig），每个包下通过 index.ts 统一导出该域的所有接口函数与 TypeScript 类型定义。
- 列表页采用“搜索区 + 表格区 + 分页组件”的标准布局，搜索表单使用 el-form inline 模式，并支持回车触发搜索。
- 表单弹窗组件通过 defineExpose 暴露 open 方法供父组件调用，并通过 emit('success') 通知父组件刷新列表，实现组件解耦。
- 涉及删除等敏感操作时，统一使用 message.delConfirm() 进行二次确认，并在操作成功后提供统一的 success 提示。