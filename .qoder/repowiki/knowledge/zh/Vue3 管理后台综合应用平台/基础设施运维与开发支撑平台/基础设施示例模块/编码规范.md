- API 模块统一导出命名空间对象（如 `Demo01ContactApi`），内部封装分页查询、详情获取、增删改及 Excel 导出等标准异步方法。
- 列表页采用统一的交互范式：顶部为搜索过滤栏，中部为带分页的数据表格，底部通过 `ref` 调用表单组件完成新增或编辑操作。
- 表单组件通过 `defineExpose` 暴露 `open` 方法以控制弹窗显示，并在提交成功后触发 `success` 事件通知父组件刷新列表。