- API 定义层统一导出 TypeScript 接口（VO）与异步请求函数，并存放于 `src/api/opshub/[module]/index.ts`。
- 视图层遵循“搜索栏 + 操作栏 + 数据表格 + 分页”的标准布局，并通过 `ContentWrap` 组件进行视觉分块。
- 跨模块引用时，统一调用目标模块提供的 `getSimple[Entity]List` 接口获取下拉选项数据，避免重复定义实体结构。