- 组件采用 'src' 目录结构配合 'index.ts' 统一导出的组织方式，实现内部实现与外部引用的解耦。
- 自定义 Hook 统一以 'use' 前缀命名（如 useTable, useCrudSchemas），并存放于 hooks/web 或 hooks/event 目录下。
- 全局功能模块（如插件、指令、状态管理）均提供名为 'setup' 开头的初始化函数（如 setupStore, setupAuth），在 main.ts 中集中调用。