- API 模块采用统一的 `request` 封装，并根据功能聚合为对象（如 `ProcessExpressionApi`）或独立导出函数（如 `createLeave`）。
- 列表页均包含搜索栏、数据表格及分页组件，并通过 `getList` 方法统一处理加载状态与数据刷新。
- 表单弹窗组件通过 `defineExpose({ open })` 暴露打开方法，并利用 `emit('success')` 通知父组件刷新列表。
- 字典数据通过 `getIntDictOptions` 或 `getStrDictOptions` 工具函数结合 `DICT_TYPE` 常量进行动态渲染。