- **模块化路由与状态隔离**：采用 `src/api` 按业务域（如 `opshub`, `bpm`, `ai`）划分 API 契约，配合 `src/store/modules` 实现各业务模块的状态独立管理。
- **标准化布局壳层**：通过 `src/layout` 提供统一的侧边栏、面包屑及标签页导航，利用 `v-hasPermi` 指令在壳层级别实现细粒度的功能权限控制。
- **共享基础设施**：所有子模块复用 `src/config/axios` 进行统一的请求拦截、错误码处理及 Token 注入，并通过 `src/components` 沉淀通用的表格、表单及弹窗组件。