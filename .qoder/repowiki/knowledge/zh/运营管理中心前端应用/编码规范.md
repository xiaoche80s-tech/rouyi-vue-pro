- API 层采用 `index.ts` 统一定义接口函数与 TypeScript 类型，并遵循 `get/create/update/delete` 的命名规范。
- 视图层采用 `ContentWrap` 容器包裹搜索栏与数据表格，并统一集成 `Pagination` 组件处理分页逻辑。
- 业务页面遵循“列表页 + 表单弹窗”的交互模式，通过 `ref` 引用子组件实例触发 `open` 方法实现增改操作。