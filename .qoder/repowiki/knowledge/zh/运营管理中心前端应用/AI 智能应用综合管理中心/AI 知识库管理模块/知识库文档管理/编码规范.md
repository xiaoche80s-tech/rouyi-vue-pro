- 使用 `inject('parent')` 或 `getCurrentInstance()?.parent` 在子步骤组件中调用父容器的导航方法（如 `goToNextStep`）。
- 在 `ProcessStep.vue` 中使用 `setTimeout` 实现递归轮询以获取实时处理进度，并在组件卸载时清除定时器。
- 通过 `v-model` (modelValue) 在向导步骤间共享和同步复杂的表单状态（如文件列表、分段配置）。