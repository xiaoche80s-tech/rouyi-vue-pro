- 通过 `api/` 包暴露 `BpmProcessInstanceApi` 等接口，实现与系统其他模块（如 OA、CRM）的解耦调用。
- 利用 `framework/flowable` 深度定制 Flowable 引擎，通过策略模式（Strategy Pattern）动态计算审批人，并注册全局事件监听器同步业务状态。
- 采用分层架构：`controller` 处理 HTTP 请求，`service` 编排业务逻辑，`dal` 负责持久化，并通过 `convert` 层实现 DO/VO/DTO 的自动化转换。
- 引入 `oa_leave` 作为标准业务示例，演示如何将自定义业务数据与 BPMN 流程实例 ID 绑定及状态联动。