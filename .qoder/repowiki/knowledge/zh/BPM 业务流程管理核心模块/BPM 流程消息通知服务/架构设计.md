- 采用接口与实现分离模式，`BpmMessageService` 定义通知入口，`BpmMessageServiceImpl` 负责具体逻辑。
- 依赖 `SmsSendApi` 进行底层短信发送，并通过 `BpmMessageConvert` 转换用户 ID 与模板参数。
- 使用 `BpmMessageEnum` 管理不同场景对应的短信模板代码，确保业务场景与模板解耦。
- 内部封装 `getProcessInstanceDetailUrl` 方法，结合 `WebProperties` 动态生成管理后台的流程详情链接。