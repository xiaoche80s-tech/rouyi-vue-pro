- 业务模块监听 BPM 流程状态时，需继承 `BpmProcessInstanceStatusEventListener` 并实现 `getProcessDefinitionKey` 以完成特定流程的过滤与回调。
- 跨模块数据交互优先通过 `api` 包下的 DTO 与 Feign/本地 API 接口进行，禁止直接依赖其他模块的 Service 实现类。
- 所有业务实体操作需遵循 `Controller -> Service -> Mapper` 分层规范，并在 Service 层通过 `@Validated` 开启参数校验。