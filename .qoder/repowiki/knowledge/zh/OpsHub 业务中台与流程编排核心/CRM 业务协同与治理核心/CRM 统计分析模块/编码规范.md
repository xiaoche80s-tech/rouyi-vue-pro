- 统计接口统一采用“先获取基础统计数据，再在内存中按时间区间或用户维度合并聚合”的处理模式，以适配不同的时间粒度（日/周/月）。
- 所有按用户维度的统计结果，均通过 `appendUserInfo` 或类似逻辑，调用 `AdminUserApi` 批量获取用户昵称并填充至响应 VO，避免 N+1 查询。
- 查询参数中的 `userId` 为空时，自动降级为根据 `deptId` 递归获取子部门下所有用户 ID 列表，实现部门维度的统计透视。