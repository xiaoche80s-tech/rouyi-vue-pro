- 分层架构：遵循 Controller-Service-Mapper-DO 标准四层结构，通过 VO 对象隔离前后端数据模型。
- 核心实体：包含 CrmProductDO（产品信息）与 CrmProductCategoryDO（产品分类），分类支持两级层级结构（parentId）。
- 数据访问：基于 MyBatis-Plus 的 BaseMapperX 扩展，利用 MPJLambdaWrapperX 实现动态条件查询与分页逻辑。
- 业务逻辑：Service 层提供产品的增删改查、状态过滤、有效性校验以及分类的树形列表获取能力。