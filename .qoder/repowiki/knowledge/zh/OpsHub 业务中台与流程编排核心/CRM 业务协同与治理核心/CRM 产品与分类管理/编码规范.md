- VO 对象按功能域分包命名，如 vo/product 和 vo/category，并严格区分 ReqVO（输入）与 RespVO（输出）。
- Mapper 接口继承 BaseMapperX，并通过 default 方法封装复杂的 Lambda 查询条件，保持 XML 文件零配置。
- Controller 层统一使用 @PreAuthorize 进行细粒度权限控制，并配合 @TransMethodResult 实现字典或用户名的自动翻译。