- VO/BO/DO 转换统一使用 `BeanUtils.toBean` 进行对象映射。
- 跨模块用户信息查询通过 `AdminUserApi` 远程调用，并在 Controller 层通过 `MapUtils.findAndThen` 进行内存组装，避免 N+1 查询。
- 业务类型枚举 `CrmBizTypeEnum` 用于区分跟进记录所属的主业务实体（如客户、商机），并作为权限校验和数据路由的依据。