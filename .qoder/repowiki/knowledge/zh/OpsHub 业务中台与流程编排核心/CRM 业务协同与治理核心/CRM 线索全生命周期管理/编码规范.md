- Service 层写操作方法统一使用 `@Transactional` 保证原子性，并结合 `@LogRecord` 记录业务审计日志。
- 数据查询接口在 Mapper 层通过 `MPJLambdaWrapperX` 结合 `CrmPermissionUtils` 动态注入数据权限过滤条件。
- VO 与 DO 之间的转换统一使用 `BeanUtils.toBean` 或 `BeanUtils` 工具类进行属性拷贝。