- 服务层方法使用`@LogRecord`注解记录业务操作日志，并通过`LogRecordContext`传递上下文变量以填充日志模板。
- 查询接口返回VO时，在Controller层通过`buildContractDetail`等方法批量加载关联实体（客户、用户、联系人等）并组装，避免N+1查询问题。
- 涉及多步数据库操作或外部调用（如BPM发起）的业务方法，使用`@Transactional(rollbackFor = Exception.class)`确保事务原子性。