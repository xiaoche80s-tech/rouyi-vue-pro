- 操作日志解析函数统一实现 `IParseFunction` 接口，使用 `@Component` 注册，并定义 `public static final String NAME` 作为函数标识。
- AOP 切面类统一使用 `@Aspect` 和 `@Component` 注解，并通过 `@Before("@annotation(...)")` 绑定自定义注解进行前置校验。
- 框架配置类统一放置在 `web.config` 包下，使用 `@Configuration(proxyBeanMethods = false)` 优化 Bean 创建性能。