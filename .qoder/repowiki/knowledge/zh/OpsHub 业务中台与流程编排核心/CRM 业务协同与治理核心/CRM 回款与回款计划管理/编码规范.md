- Controller 层通过私有方法（如 `buildReceivableDetailList`）批量组装 VO，利用 Map 预加载关联实体（客户、用户、合同）以避免 N+1 查询问题。
- Service 层使用 `@LogRecord` 注解配合 `LogRecordContext` 记录业务操作日志，并通过 SpEL 表达式引用业务对象属性。
- 涉及多步数据修改的业务方法（如创建回款并更新计划）均标注 `@Transactional(rollbackFor = Exception.class)` 以确保事务原子性。