- 业务枚举类统一实现 `ArrayValuable<Integer>` 接口并提供静态 `ARRAYS` 常量，以支持框架层面的枚举值数组化操作。
- 错误码遵循 `1_020_XXX_YYY` 的分段命名规范，并按业务实体（如合同、线索、商机）进行分组定义。
- 枚举类普遍采用 `@RequiredArgsConstructor` 或 `@AllArgsConstructor` 配合 `@Getter` 的 Lombok 模式构建不可变枚举结构。