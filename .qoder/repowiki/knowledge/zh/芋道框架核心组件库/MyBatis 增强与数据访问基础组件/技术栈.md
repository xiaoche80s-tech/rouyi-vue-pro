- **ORM 框架**：基于 `mybatis-plus-spring-boot3-starter` 和 `mybatis-plus-join-boot-starter` 实现增强 ORM 能力。
- **多数据源**：集成 `dynamic-datasource-spring-boot3-starter` 支持动态数据源切换。
- **连接池**：使用 `druid-spring-boot-3-starter` 作为数据库连接池。
- **数据翻译**：采用 `easy-trans-spring-boot-starter` 实现 VO 层的数据字典/关联翻译。