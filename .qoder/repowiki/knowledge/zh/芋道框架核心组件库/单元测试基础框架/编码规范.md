- 所有涉及数据库操作的测试基类均配置`@Sql(scripts = "/sql/clean.sql", executionPhase = AFTER_TEST_METHOD)`以确保用例间的数据隔离。
- 测试环境统一激活`unit-test` Profile，通过`@ActiveProfiles("unit-test")`加载专用配置文件。
- 随机生成POJO时，针对特定字段名（如status, deleted, type）在Podam策略中预设符合业务逻辑的默认值范围。