- 安全相关 Bean 命名遵循语义化缩写，如将 SecurityFrameworkService 注册为 "ss" 以便快速引用。
- 权限校验方法统一采用 hasPermission/hasRole 单数形式委托给 hasAnyPermissions/hasAnyRoles 变长参数方法实现。
- 过滤器中捕获异常后直接通过 GlobalExceptionHandler 处理并返回 JSON 响应，中断后续过滤链执行。