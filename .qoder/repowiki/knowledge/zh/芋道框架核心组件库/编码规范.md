- 所有 Starter 模块必须包含 `config` 包用于存放 `@AutoConfiguration` 配置类，以及 `core` 包用于存放核心业务或技术实现。
- 业务相关的功能组件模块命名必须包含 `biz` 标识（如 `yudao-spring-boot-starter-biz-tenant`），以区别于纯技术框架组件。
- 全局通用的常量、枚举、异常及 POJO 必须统一定义在 `yudao-common` 模块中，禁止在各 Starter 中重复定义基础契约。