- 统一使用 `CommonResult<T>` 作为接口返回包装，通过 `success/error` 静态工厂方法构建。
- 错误码采用对象化 `ErrorCode` 封装，区分全局码与业务码范围，支持消息格式化。
- 工具类（如 `JsonUtils`、`CollectionUtils`）采用静态方法聚合，内部维护共享实例或委托给第三方库。
- 自定义校验注解（如 `@InEnum`、`@Mobile`）配套实现 `ConstraintValidator`，并依托 `ArrayValuable` 接口枚举合法值。