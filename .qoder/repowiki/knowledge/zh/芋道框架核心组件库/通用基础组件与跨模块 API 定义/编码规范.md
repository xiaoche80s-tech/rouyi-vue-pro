- 统一使用 `CommonResult<T>` 包装接口返回值，并通过 `error()`/`success()` 静态方法构建实例。
- 业务异常统一抛出 `ServiceException`，配合 `ErrorCode` 枚举实现错误码与提示信息的解耦。
- 跨模块 API 接口统一以 `CommonApi` 结尾，并放置在 `cn.iocoder.yudao.framework.common.biz` 包下。
- 工具类统一放置在 `util` 子包中，命名以 `Utils` 结尾，并提供静态方法访问。