- 各业务模块（如 opshub, im）均遵循 Controller-Service-Mapper 分层结构，并在 module 根目录下通过 package-info.java 声明模块边界。
- 跨模块通信优先通过 API 接口层（如 system.api, bpm.api）进行解耦，避免直接引用其他模块的 Service 实现类。
- 数据权限过滤逻辑统一封装在 framework 的 DataPermissionRule 接口实现中，业务代码通过 @DataPermission 注解声明式启用。