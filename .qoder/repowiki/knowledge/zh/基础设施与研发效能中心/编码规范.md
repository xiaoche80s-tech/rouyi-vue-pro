- 所有子功能模块均遵循 Controller-Service-Mapper 分层架构，并在 `controller/admin/` 下按业务域分包。
- 跨模块调用的 API 接口统一定义在 `api/` 包下，并配套提供 `ApiImpl` 实现类以隔离内部逻辑。
- 数据对象（DO）与视图对象（VO）之间的转换统一使用 MapStruct 框架，并在 `convert/` 包下定义对应的 Convert 接口。