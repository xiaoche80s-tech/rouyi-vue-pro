- 存储客户端实现统一继承 `AbstractFileClient` 并实现 `FileClient` 接口，确保 upload, delete, getContent 等行为的一致性。
- 文件上传支持两种模式：模式一为后端接收流后转发至存储源，模式二为后端生成预签名地址由前端直传存储源。
- 所有文件操作均关联 `configId`，通过配置编号动态路由到对应的存储客户端实例，实现多租户或多环境下的存储隔离。