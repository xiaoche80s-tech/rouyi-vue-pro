- API 实现类与 Controller 均使用 `@Validated` 开启参数校验，Service 接口定义清晰的分页与清理契约。
- DO 实体统一继承 `BaseDO` 并采用 Lombok 链式注解（`@Builder`、`@NoArgsConstructor`、`@AllArgsConstructor`）。
- 管理后台接口统一返回 `CommonResult<T>`，并通过 `BeanUtils.toBean` 进行 DO 到 VO 的转换。
- 定时清理 Job 实现 `JobHandler` 接口，使用常量定义保留天数与单次删除上限，避免数据库压力过大。