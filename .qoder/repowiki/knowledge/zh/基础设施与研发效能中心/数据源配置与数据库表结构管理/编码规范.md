- 敏感字段加密：在 DO 层通过 @TableField(typeHandler = EncryptTypeHandler.class) 注解声明，实现数据库存取时的自动加解密。
- 主从数据源兼容：在 Service 层通过常量 ID_MASTER (0L) 识别主数据源请求，动态从 DynamicDataSourceProperties 中构建主数据源配置对象。
- VO 与 DO 转换：统一使用 BeanUtils.toBean 在 Controller 层完成 SaveReqVO/RespVO 与 DO 之间的属性拷贝。