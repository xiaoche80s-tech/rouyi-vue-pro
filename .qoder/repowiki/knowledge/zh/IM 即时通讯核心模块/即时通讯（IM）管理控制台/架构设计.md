- **分层架构**：采用 `api`（数据契约）、`views/home`（C端用户交互）、`views/manager`（B端运营审计）的三层结构。
- **状态驱动**：通过 Pinia Store（如 `messageStore`, `conversationStore`）统一管理 WebSocket 实时流与本地持久化数据，实现视图与数据的解耦。
- **逻辑复用**：利用 Composables（如 `useMessageSender`, `useLiveKitRoom`）封装复杂的媒体处理与消息发送逻辑，在用户聊天与后台管理中共享基础能力。