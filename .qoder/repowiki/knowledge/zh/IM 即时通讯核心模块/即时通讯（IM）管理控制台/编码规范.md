- API 模块按业务域（friend, group, message）及角色（manager）进行目录隔离，统一使用 index.ts 导出请求方法。
- 视图层采用“页面入口 + components 组件库 + composables 逻辑钩子”的组合模式，实现高内聚低耦合。
- 复杂交互逻辑（如 RTC 通话、消息多选）被抽取为独立的 Composables，避免在 Vue 组件中堆积业务代码。