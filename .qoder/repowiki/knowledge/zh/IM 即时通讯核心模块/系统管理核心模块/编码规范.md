- 所有列表页均采用 `ContentWrap` 包裹搜索栏与数据表格，保持视觉一致性。
- 增删改查操作后统一调用 `getList()` 刷新数据，并使用 `message.success` 或 `message.delConfirm` 进行用户反馈。
- API 接口定义文件（index.ts）中，VO 接口与 CRUD 方法成对出现，且方法名遵循 `get/create/update/delete` 语义。
- 视图组件中通过 `defineOptions({ name: 'SystemXxx' })` 显式声明组件名称，便于调试与路由匹配。