- API层：`api/bpm/processInstance/index.ts` 定义流程实例分页查询、创建、取消、详情获取及BPMN视图等接口。
- 视图层：`views/bpm/processInstance/` 采用模块化结构，包含列表页（`index.vue`）、详情页（`detail/`）、创建页（`create/`）及管理报表页。
- 详情页设计：`detail/index.vue` 作为核心容器，通过 Tab 切换展示审批表单（支持动态表单与业务表单）、流程图（支持简单模式与BPMN模式）及流转记录，并集成时间线与操作按钮组件。