- API 接口定义遵循统一的异步函数封装规范，使用 `request.get/post/put/delete` 并显式声明返回类型或泛型。
- 表单弹窗与抽屉组件均通过 `defineExpose` 暴露 `open` 方法供父组件调用，实现组件内部状态（如可见性、加载态）的自治。
- 字典数据与常量引用统一从 `@/utils/dict` 和 `@/utils/constants` 导入，确保前端展示值与后端枚举的一致性。