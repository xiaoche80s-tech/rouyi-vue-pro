- API 模块采用命名导出函数配合 TypeScript 接口定义（VO），统一使用 `request` 实例进行 HTTP 调用。
- 表单组件通过 `defineExpose` 暴露 `open` 方法供父组件控制显隐及初始化数据，并通过 `emit` 事件回调处理成功后的业务逻辑。
- 列表页与表单页分离，复杂操作（如经销商绑定）抽取为独立子组件，通过 `ref` 实例调用其内部方法实现模块化交互。