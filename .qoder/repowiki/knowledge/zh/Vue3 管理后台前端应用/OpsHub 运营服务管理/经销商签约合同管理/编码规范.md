- 弹窗组件统一暴露 `open` 方法用于初始化状态和显示，并通过 `defineEmits` 触发 `success` 事件通知父组件刷新数据。
- 业务操作成功后，统一调用 `message.success` 提示用户，并在 `try...finally` 块中管理 `loading` 或 `submitting` 状态以确保 UI 响应性。
- 表格行操作根据合同状态（如 `status` 和 `subStatus`）动态显示不同的功能按钮（如“去签署”或“上传盖章文件”），以匹配当前业务流程节点。