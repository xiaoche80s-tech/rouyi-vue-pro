- API 模块遵循目录隔离规范，每个业务实体（如 csTask）在 `src/api/opshub` 下拥有独立文件夹及 `index.ts` 出口文件。
- 业务枚举（如状态、类型）在组件内部定义为常量数组（如 `statusOptions`），并通过辅助函数（如 `getStatusLabel`）进行映射转换。
- 列表页与详情页均集成统一的权限判断逻辑，根据当前用户 ID 与角色（如 `isAdmin`）动态渲染操作按钮。
- 实时业务场景（如咨询）统一使用 `useCsWebSocket` Hook 处理消息订阅、状态同步及新消息高亮提示。