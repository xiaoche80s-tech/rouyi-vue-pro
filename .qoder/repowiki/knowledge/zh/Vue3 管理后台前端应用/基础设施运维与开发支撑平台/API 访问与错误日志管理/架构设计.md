- 采用 API 层与视图层分离架构，`src/api/infra` 下定义数据接口与请求方法，`src/views/infra` 下实现页面逻辑。
- 视图层由列表页（`index.vue`）和详情页（`Detail.vue`）组成，列表页负责搜索、分页、导出及状态操作，详情页通过 `Dialog` 组件展示完整日志信息。
- 依赖 `@/config/axios` 进行 HTTP 请求，使用 `el-table`、`el-form` 等 Element Plus 组件构建 UI，通过 `v-hasPermi` 指令控制权限。