- 列表页与详情页分离，详情页通过 `ref` 调用 `open` 方法展示数据，避免路由跳转。
- API 请求函数统一封装在 `api` 目录下的 `index.ts` 文件中，按模块划分。
- 使用 `reactive` 定义查询参数对象，并在重置时调用 `resetFields` 清空表单。
- 导出操作前通过 `message.exportConfirm()` 进行二次确认，并使用 `download.excel` 处理文件下载。