- 所有视图组件均使用 `doc-alert` 组件在顶部展示关联的技术文档链接，提供上下文帮助。
- 除 WebSocket 外，其他监控页面均遵循“加载状态控制 + IFrame 嵌入”的标准模板结构，使用 `v-loading` 处理异步配置获取过程。
- 组件命名遵循 `Infra[ToolName]` 规范（如 `InfraDruid`, `InfraSkyWalking`），保持模块内标识符的一致性。