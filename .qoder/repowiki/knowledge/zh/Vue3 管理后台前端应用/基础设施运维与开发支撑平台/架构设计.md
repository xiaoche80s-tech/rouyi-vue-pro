- **统一通信契约**：所有子模块均依赖 `@/config/axios` 封装的请求实例，遵循 RESTful 风格（如 `/infra/{module}/page`）进行数据交互。
- **标准化视图架构**：采用“搜索栏 + 数据表格 + 分页组件”的固定布局模式，并通过 `v-hasPermi` 指令在 UI 层统一实施权限控制。
- **模块化路由组织**：各功能域在 `src/views/infra/` 下独立成包，通过懒加载或动态路由注册集成至主应用，实现业务逻辑的物理隔离。