- 采用响应式引用（ref）配合 defineExpose 暴露 open 方法的方式管理弹窗组件的生命周期与显隐状态。
- 在表格操作列中利用 v-hasPermi 指令结合业务状态（如 status/subStatus）动态控制按钮的可见性与可用性，实现细粒度的权限与流程控制。
- 使用 TypeScript Interface (VO) 统一定义 API 请求参数与响应数据结构，确保前后端数据交互的类型安全。