- 所有 AI 子模块均遵循“views/ai/[feature]/index”作为用户入口、“views/ai/[feature]/manager”作为管理后台的目录组织规范。
- 跨模块共享的枚举定义（如平台类型、模型类别）集中收敛于 `views/ai/utils/constants.ts`，避免在各子模块中重复定义。
- API 接口定义严格映射业务领域，在 `api/ai/` 下按功能域（chat, image, knowledge 等）划分子目录并导出统一的 index.ts。