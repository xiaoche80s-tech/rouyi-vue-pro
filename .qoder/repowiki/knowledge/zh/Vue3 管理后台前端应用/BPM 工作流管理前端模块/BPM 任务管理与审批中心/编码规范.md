- 视图组件均采用 Composition API (`<script setup>`) 编写，并使用 `defineOptions` 显式声明组件名称（如 `BpmTodoTask`）。
- 列表页统一使用 `reactive` 定义查询参数对象，并配合 `ref` 管理的表单引用实现搜索、重置及分页联动逻辑。
- 所有涉及时间展示的列均通过 `:formatter` 绑定 `dateFormatter` 工具函数，确保时间格式的一致性。
- 页面顶部统一嵌入 `<doc-alert>` 组件，提供指向相关业务流程文档的快捷链接，增强功能可发现性。