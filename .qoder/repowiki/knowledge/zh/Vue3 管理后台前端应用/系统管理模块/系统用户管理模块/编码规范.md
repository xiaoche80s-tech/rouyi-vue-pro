- 采用 Dialog 弹窗模式处理表单交互（如 UserForm、UserAssignRoleForm），通过 defineExpose 暴露 open 方法控制显隐。
- API 接口定义与 TypeScript 接口（VO）分离，在 `index.ts` 中统一导出请求方法与类型定义。
- 列表页操作列使用 el-dropdown 收纳低频操作（如重置密码、分配角色），高频操作（如修改）直接展示。
- 用户选择器组件支持单选（radio）与多选（checkbox）模式切换，并通过 row-key 和 reserve-selection 实现跨页选中保持。