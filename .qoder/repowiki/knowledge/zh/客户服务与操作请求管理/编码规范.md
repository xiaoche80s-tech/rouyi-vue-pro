- 工单与操作请求编号均采用“前缀-日期-序列号”格式（如 TASK-20260616-001），并在 Service 层通过查询当日最大序列号实现并发安全的自增生成。
- 所有涉及状态变更的操作（如接单、提交、验收）均在 Service 层执行严格的前置状态校验（validateStatus），确保状态机流转的合法性与幂等性。
- 分页查询接口统一在 Service 层解析当前用户角色，动态注入 viewScope 参数，从而实现不同角色（如经销商仅见己方、执行员可见未接单）的数据隔离。