- 子表单组件（如 BasicInfoForm）通过 defineExpose 暴露 validate 方法，由父组件（EditTable）在提交前统一触发校验。
- 弹窗类组件（如 ImportTable、PreviewCode）统一采用 open/close 模式控制显隐，并通过 defineEmits 向父组件传递操作结果。
- API 请求函数统一使用 request.get/put/post 封装，URL 路径遵循 /infra/codegen/{action} 规范，参数通过 params 或 data 传递。