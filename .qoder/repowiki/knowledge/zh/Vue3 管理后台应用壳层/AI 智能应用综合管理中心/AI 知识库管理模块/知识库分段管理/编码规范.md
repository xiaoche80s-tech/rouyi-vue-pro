- 列表页通过路由参数（如`route.query.documentId`）初始化查询条件，并在`onMounted`中校验必要参数的存在性，缺失时执行错误提示与路由跳转。
- 状态切换操作（如启用/禁用）采用乐观更新策略，在用户确认失败后手动恢复前端状态值，确保UI与后端数据一致性。
- 表单弹窗组件统一使用`defineExpose`暴露`open`方法，接收操作类型（create/update）及业务ID，内部根据类型决定是重置表单还是加载详情数据。