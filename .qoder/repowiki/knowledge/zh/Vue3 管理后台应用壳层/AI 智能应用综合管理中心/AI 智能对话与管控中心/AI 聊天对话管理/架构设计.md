- 入口组件 `index.vue` 使用 `el-tabs` 将 `ChatConversationList`（会话列表）与 `ChatMessageList`（消息列表）组织为两个标签页。
- 子组件各自封装独立的搜索表单、数据表格与分页逻辑，通过 `ChatConversationApi` / `ChatMessageApi` 调用后端接口。
- 两个列表组件均在 `onMounted` 中加载系统用户列表用于展示用户昵称，并在删除操作后刷新列表。
- 权限控制通过 `v-hasPermi` 指令限制删除按钮的可见性。