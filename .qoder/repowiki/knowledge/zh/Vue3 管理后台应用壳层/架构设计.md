- **启动编排**：在 `main.ts` 中集中初始化状态管理 (Pinia)、国际化、UI 框架及自定义指令，确保全局环境一致性。
- **权限控制**：通过 `permission.ts` 实现路由守卫，结合后端返回的动态菜单数据执行 `router.addRoute`，实现细粒度的页面级访问控制。
- **网络通信**：利用 Axios 拦截器统一处理 Token 注入、租户 ID 透传、API 加解密以及无感知的 Token 刷新机制，为所有子模块提供标准化的数据请求通道。