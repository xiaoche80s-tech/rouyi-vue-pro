- 业务枚举（如状态、类型、分类）在组件内部定义为常量数组，并提供统一的 Label/TagType 映射函数进行渲染。
- 列表页采用“筛选栏 + 操作按钮区 + 表格 + 分页”的标准布局结构，筛选条件重置时自动触发首页查询。
- 涉及状态变更的业务操作（如接单、验收、取消）均通过 ElMessageBox 进行二次确认，并在成功后刷新列表。
- API 模块按业务实体分包，每个包下的 index.ts 集中导出该实体的所有接口方法与 TypeScript 类型定义。