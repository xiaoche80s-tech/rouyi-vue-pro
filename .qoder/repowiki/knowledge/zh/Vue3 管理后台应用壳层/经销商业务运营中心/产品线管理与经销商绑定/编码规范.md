- API 接口定义与 TypeScript 类型声明（VO）合并在同一文件中，便于维护接口契约。
- 表单与详情弹窗/抽屉组件通过暴露 `open` 方法并由父组件通过 `ref` 调用，实现组件的受控显示与数据初始化。
- 使用 `v-hasPermi` 指令在前端模板中细粒度控制按钮级别的权限显示。
- 在绑定/关联场景中，前端通过并行请求获取全量候选项与已关联项，并利用 `computed` 动态计算可选项，避免后端额外提供过滤接口。