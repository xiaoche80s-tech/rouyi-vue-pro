- 采用 Modal 组件封装模式，通过 `defineExpose({ open })` 暴露打开方法，由父组件通过 `ref` 调用以控制弹窗显示及数据传递。
- 在表格行操作及弹窗底部按钮中，统一使用 `v-hasPermi` 指令进行细粒度的功能权限控制（如 `dealer:basedata:download`）。
- 有效期状态等枚举字段在前端通过映射函数（如 `getExpireStatusLabel`）转换为可读文本，并结合 `el-tag` 组件根据状态值动态设置颜色类型。