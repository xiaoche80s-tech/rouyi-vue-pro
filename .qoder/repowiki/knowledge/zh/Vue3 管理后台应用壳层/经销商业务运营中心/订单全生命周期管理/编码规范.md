- API 接口函数统一使用 `async/await` 语法，并在 API 层完成 URL 拼接与参数处理。
- 业务弹窗组件（如付款、开票）均通过 `defineExpose` 暴露 `open` 方法供父组件调用，实现受控显示。
- 金额字段在展示层统一通过 `toLocaleString('zh-CN', { minimumFractionDigits: 2 })` 进行格式化。
- 状态标签（如进度、付款状态）采用映射表（Record<string, string>）方式实现状态值到 UI 样式/文本的转换。