- API 模块统一导出 TypeScript 接口定义（VO）与异步请求方法，保持类型安全与调用一致性。
- 弹窗类组件（Modal/Dialog）均采用 `ref` 暴露 `open` 方法进行受控显示，并通过 `emit('success')` 通知父组件刷新数据。
- 文件尺寸格式化逻辑在多个附件展示组件中复用，统一采用 B/KB/MB/GB 单位自动换算并保留适当小数位。
- 列表页筛选栏与分页查询解耦，筛选条件变更时重置页码并触发父组件的搜索事件以重新加载数据。