- 所有业务数据表均继承 `TenantBaseDO`，包含 `tenant_id` 字段以支持多租户隔离。
- 采用双维度授权模型，通过 `dealer_code` 和 `product_line_code` 字段在业务表中实现数据权限过滤。
- 业务编码遵循特定前缀规则，如合同编码（MC-/POL-）、售后单号（SO-）、操作请求编号（OPR-）等。
- 状态流转采用明确的枚举定义，并在PRD中提供状态机图（如售后进度、工单状态、会话状态）。