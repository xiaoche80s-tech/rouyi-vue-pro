- 金额字段在前端展示时统一从“分”转换为“元”，通常使用 `(price / 100).toFixed(2)` 或工具函数 `fenToYuan` 进行格式化。
- 列表页均采用统一的搜索栏布局，包含重置、搜索按钮，并在表单回车键上绑定搜索事件以提升操作效率。
- 所有涉及敏感操作（如删除、导出、状态变更）的功能点均通过 `v-hasPermi` 指令进行细粒度的权限控制。
- 日期范围选择器统一配置默认时间为当天的 `00:00:00` 至 `23:59:59`，确保时间筛选的准确性。