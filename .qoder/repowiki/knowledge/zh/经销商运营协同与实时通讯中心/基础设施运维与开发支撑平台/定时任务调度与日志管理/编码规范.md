- API 接口定义统一放置在 `src/api` 对应业务目录下，采用 TypeScript Interface 定义数据模型（如 `JobVO`），并导出具名函数供视图层调用。
- 表单操作（新增/修改）统一封装在独立的 `*Form.vue` 组件中，通过 `defineExpose` 暴露 `open` 方法，由父组件控制弹窗显示与数据加载。
- 列表页搜索栏统一使用 `el-form` 配合 `inline` 布局，并提供重置（`resetFields`）与搜索（重置页码后刷新列表）的标准交互逻辑。