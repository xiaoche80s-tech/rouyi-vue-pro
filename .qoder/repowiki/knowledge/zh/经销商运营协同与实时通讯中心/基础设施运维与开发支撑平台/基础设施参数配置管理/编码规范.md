- API 模块采用命名导出方式组织请求函数，并统一使用 `request` 封装的 HTTP 客户端进行调用。
- 视图组件遵循“搜索-列表-弹窗”布局模式，使用 `ContentWrap` 包裹内容区域，并通过 `ref` 联动表单与列表逻辑。
- 字典数据通过 `DICT_TYPE` 常量配合 `getIntDictOptions` 或 `getBoolDictOptions` 工具函数动态渲染为下拉选项或标签。
- 表单校验规则在 `ConfigForm.vue` 中通过 `reactive` 定义，并对必填字段设置 `blur` 触发机制以确保用户体验。