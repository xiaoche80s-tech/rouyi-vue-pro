- API 层统一导出类型定义（VO）与请求函数，并存放于 `src/api/infra/{module}/index.ts`。
- 视图层采用 Composition API (`<script setup>`)，并使用 `reactive` 管理查询参数，`ref` 管理列表数据与加载状态。
- 批量操作前必须通过 `handleRowCheckboxChange` 收集选中 ID，并在执行前调用 `message.delConfirm()` 进行二次确认。