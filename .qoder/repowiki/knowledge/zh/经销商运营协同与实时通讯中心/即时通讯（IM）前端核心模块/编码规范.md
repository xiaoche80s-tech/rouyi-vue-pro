- 所有业务 Store 均实现 `load...Data` 方法从 IndexedDB 恢复本地缓存，并在 `fetch...List` 成功后调用 `save...List` 进行持久化同步。
- WebSocket 消息处理采用 Dispatcher 模式，在 `websocketStore` 中解析帧类型并分发至对应业务 Store 的 `apply...Notification` 方法进行处理。
- 消息发送采用乐观更新策略，先在 `messageStore` 插入 SENDING 状态占位消息，待接口返回后通过 `ackMessage` 合并服务端真实状态。