- 组件采用 `src/index.ts` 统一导出模式，内部通过 `src/ComponentName.vue` 实现具体逻辑，实现实现细节隐藏。
- Pinia Store 遵循“状态定义、Getters 计算、Actions 异步”的三段式结构，并提供 `useXxxStoreWithOut` 辅助函数以在非组件上下文中使用。
- 布局组件广泛使用 TSX 语法进行动态渲染，通过 `computed` 属性驱动样式类名与组件显隐，实现高性能的响应式布局切换。