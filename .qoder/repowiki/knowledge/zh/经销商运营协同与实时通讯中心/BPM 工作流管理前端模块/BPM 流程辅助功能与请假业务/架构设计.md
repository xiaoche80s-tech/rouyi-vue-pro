- 采用 API 层与视图层分离架构，`src/api/bpm/` 下按功能模块（leave, processExpression, processListener, simple）组织 RESTful 接口调用。
- 视图层 `src/views/bpm/` 对应各功能模块，遵循“列表页 + 表单弹窗/独立页”的交互模式，如 `processExpression/index.vue` 配合 `ProcessExpressionForm.vue`。
- 引入 Pinia 状态管理（`src/store/modules/bpm/simpleWorkflow.ts`）维护简易流程设计器的 UI 状态（如节点配置、抽屉显隐），实现跨组件状态共享。
- 业务组件化：请假创建页 `create.vue` 集成 `ProcessInstanceTimeline` 组件展示审批节点预测，并通过 `SimpleProcessDesigner` 组件支持可视化流程建模。