- 采用 defineModel 实现子组件与父组件的双向数据绑定，并在保存时通过展开运算符创建新对象以触发响应式更新。
- 在向导式表单中，各步骤组件通过 defineExpose 暴露 validate 方法，由父组件统一编排跨步骤的校验流程。
- 使用 provide/inject 组合在深层嵌套的设计器组件（如 ProcessDesign）与顶层表单容器之间共享核心业务数据。
- API 接口定义采用 TypeScript 类型别名（如 ModelVO, ProcessDefinitionVO）明确请求与响应的数据结构。