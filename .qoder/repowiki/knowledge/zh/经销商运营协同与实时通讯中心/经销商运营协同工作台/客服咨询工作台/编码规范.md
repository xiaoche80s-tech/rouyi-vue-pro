- 使用 Tailwind CSS 实用类（如 flex, h-full, border-r）进行布局与样式定义，减少自定义 CSS。
- 遵循 Vue 3 Composition API 规范，使用 `<script setup>` 语法糖及 `ref/computed/watch` 管理逻辑。
- 子组件通过 `defineProps` 接收数据，并通过 `defineEmits` 或 `defineExpose` 暴露方法（如 appendMessage）以实现父子通信。