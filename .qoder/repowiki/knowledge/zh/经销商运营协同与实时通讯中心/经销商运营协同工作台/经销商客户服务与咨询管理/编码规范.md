- 所有 API 模块均采用统一的异步函数导出模式，并定义对应的 TypeScript 接口（VO）以规范数据结构。
- 列表查询组件普遍采用“筛选栏 + 表格 + 分页”的标准布局，并通过 `reactive` 对象统一管理查询参数。
- 业务状态变更操作（如接单、提交、验收）均通过 `ElMessageBox` 进行二次确认，并在成功后触发列表重载。
- 枚举常量（如状态、类型、来源模块）在组件内部定义为静态数组，并提供辅助函数用于标签文本与样式的映射。