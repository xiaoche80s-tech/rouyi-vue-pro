- API层：在 `src/api/opshub/dealer/` 和 `src/api/opshub/dealerScope/` 中分别定义经销商基础信息与授权范围的HTTP请求接口。
- 视图层：`src/views/opshub/dealer/` 实现经销商列表管理与表单编辑；`src/views/opshub/scope/` 采用“表格展示+抽屉新增”模式，处理用户维度的授权数据配置。
- 数据交互：前端通过组合API接口实现授权数据的结构化展示与动态更新，支持按角色过滤可选用户。