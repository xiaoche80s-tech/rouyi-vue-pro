- 采用 Composable 模式（如 useCsWebSocket, useCsConsult）封装复杂交互与业务逻辑，实现跨组件复用。
- 使用 Teleport 将聊天窗口渲染至 body，确保其在层级最高的 z-index 上显示且不受父容器 overflow 限制。
- 遵循 API 层与视图层分离原则，在 views 目录下按业务域（opshub, system, bpm 等）组织模块化代码。