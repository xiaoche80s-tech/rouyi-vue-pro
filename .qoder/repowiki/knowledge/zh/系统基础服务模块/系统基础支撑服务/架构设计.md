- 分层架构：Controller（Admin/App双端）-> Service -> DAL (Mapper/DO)，API层通过Impl桥接Service供外部模块调用。
- 接口隔离：Admin端接口具备细粒度权限控制（@PreAuthorize），App端接口通常开放或简化（@PermitAll）。
- 扩展机制：framework/operatelog包下基于IParseFunction实现操作日志的动态字段解析（如用户ID转昵称）。
- 数据模型：针对字典、日志、通知建立独立的DO与Mapper，枚举类集中管理业务状态（如LoginResultEnum）。