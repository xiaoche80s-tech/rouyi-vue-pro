- 所有公开认证接口（如登录、注册、发送验证码）均使用 `@PermitAll` 注解显式绕过安全框架的默认鉴权拦截。
- 验证码相关接口（获取与校验）统一添加 `@TenantIgnore` 注解，确保在未携带租户信息或跨租户场景下仍可正常访问。
- 请求参数校验采用嵌套验证模式，登录/注册 VO 继承自 `CaptchaVerificationReqVO`，在 Service 层通过 `Validator` 触发分组校验。