- 采用 VO/DO/DTO 分离模式，Controller 层使用 ReqVO/RespVO，Service 层操作 DO，API 层暴露 DTO。
- 在批量查询关联数据（如用户分页查询关联部门信息）时，先提取 ID 集合批量查询，再组装 Map 进行内存拼接以减少数据库交互。
- API 实现类中针对指定 ID 的查询方法通常禁用数据权限拦截（@DataPermission(enable = false)），确保跨模块调用的数据完整性。