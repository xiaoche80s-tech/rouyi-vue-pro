- 数据库对象命名遵循 `ops_` 前缀约定（如 `ops_cs_session`, `ops_dealer_info`），以区分业务模块与系统基础表。
- SQL脚本按功能分支归档，每个分支包含独立的 `_ddl.sql`（结构定义）和 `_dml.sql`（数据初始化/权限配置）文件。
- PRD文档采用统一的Step编号体系（Step 1-8），并在文档头部明确标注版本、日期、前置文档及技术约束。