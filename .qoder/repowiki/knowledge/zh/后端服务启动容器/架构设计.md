- **容器化架构**：本质上是一个“空壳”主项目，通过 Maven 依赖引入 `yudao-module-system`、`yudao-module-bpm` 等业务模块，实现功能的插拔式组装。
- **入口设计**：`YudaoServerApplication` 作为唯一启动类，通过动态包扫描路径加载各模块组件。
- **兜底路由**：`DefaultController` 提供针对未启用模块（如 ERP、CRM）的友好 404 提示，明确指引开启方式。
- **配置中心**：`application.yaml` 集中管理数据库、Redis、消息队列及 AI 大模型等全局基础设施配置。