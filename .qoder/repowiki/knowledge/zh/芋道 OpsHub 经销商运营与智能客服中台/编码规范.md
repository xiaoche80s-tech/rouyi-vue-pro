- 所有业务模块遵循分层架构：controller（接口层）、service（业务层）、dal（数据访问层）、convert（对象转换）。
- 统一使用 MapStruct 进行 DO、VO、DTO 之间的对象转换，并在 pom.xml 中配置 lombok-mapstruct-binding。
- 跨模块调用通过定义 api 包下的接口实现，禁止直接引用其他模块的 service 或 dal 层代码。
- 数据库脚本按功能分支存放在 `db/branches` 目录，命名规范为 `feature_stepX-功能描述_ddl/dml.sql`。