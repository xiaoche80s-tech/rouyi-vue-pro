采用 Service 层角色可见性过滤与数据权限叠加机制 — 采用双层过滤机制：底层由 DealerDataPermissionRule 通过 SQL 拦截器强制约束 dealer_code 和 product_line_code 的数据权限；上层在 CsTaskServiceImpl 和 CsOpReqServiceImpl 的分页查询方法中，根据当前用户角色（brand_admin/dealer/service_executor 等）动态构建额外的 WHERE 条件（如 creator_user_id = ? 或 (status = 0 OR assignee_id = ?)），两者通过 AND 逻辑叠加生效�