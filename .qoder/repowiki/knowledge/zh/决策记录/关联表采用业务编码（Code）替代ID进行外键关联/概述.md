关联表采用业务编码（Code）替代ID进行外键关联 — 在 `ops_dealer_product_line_relation`、`ops_dealer_user_scope` 和 `ops_executor_product_line_scope` 表中，将关联字段从 `dealer_id`/`product_line_id` (int8) 改为 `dealer_code`/`product_line_code` (varchar(50))�