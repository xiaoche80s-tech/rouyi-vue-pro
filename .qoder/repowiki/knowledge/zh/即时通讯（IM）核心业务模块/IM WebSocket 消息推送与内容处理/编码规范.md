- 采用静态工厂方法（如 `ofSend`, `ofRead`）构建 DTO 实例，替代传统的构造函数或 Setter 链式调用，提高代码可读性。
- 在涉及事务的业务逻辑中，强制使用 `executeAfterTransaction` 包装异步推送任务，以保证数据最终一致性。
- 对消息内容中的 URL 字段进行严格的安全校验，禁止 `javascript:`、`data:` 等危险协议前缀。
- 所有异步推送方法均包含 try-catch 异常捕获并记录错误日志，防止单个用户推送失败影响整体流程。