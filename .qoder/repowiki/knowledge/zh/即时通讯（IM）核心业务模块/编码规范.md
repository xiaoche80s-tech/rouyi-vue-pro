- 所有涉及前端实时感知的业务操作（发消息、加好友、入群）必须通过 `ImWebSocketService` 进行异步推送，且需包裹在事务提交后执行。
- Controller 层严格区分 `admin`（管理后台）与 `app`（用户端/预留）路径，管理端接口统一携带 `Manager` 后缀以区分权限边界。
- 消息体定义采用多态 DTO 模式（`TextMessage`, `ImageMessage` 等），通过 `ImMessageTypeEnum` 进行路由与序列化标识。