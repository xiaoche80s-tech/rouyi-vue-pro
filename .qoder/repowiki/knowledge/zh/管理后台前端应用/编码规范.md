- API 层采用 TypeScript Interface 定义 VO 模型，并使用 async/await 封装 RESTful 请求方法。
- 视图组件遵循 Composition API 规范，使用 `<script setup>` 语法糖并配合 `defineOptions` 声明组件名称。
- 业务模块内部采用“列表页 + 表单弹窗”的标准交互模式，并通过 `components` 子目录拆分复杂页面的逻辑块。