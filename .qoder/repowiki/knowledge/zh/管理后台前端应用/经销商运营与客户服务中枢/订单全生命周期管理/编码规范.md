- 金额字段统一使用 `formatAmount` 工具函数进行千分位格式化并保留两位小数。
- 状态标签（如进度、付款、开票）采用统一的映射表（Map/Record）管理显示文本与 UI 类型（type）。
- 业务操作按钮均通过 `v-hasPermi` 指令结合后端返回的状态字段（如 `payStatus`）进行双重权限与状态控制。
- 列表页与详情页的操作成功后，均通过触发 `refresh` 事件或调用 `refreshAll` 方法实现数据的一致性更新。