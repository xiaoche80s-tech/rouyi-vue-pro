- API 层采用 `index.ts` 集中导出 VO 接口定义与 RESTful 请求函数。
- 视图层采用“主入口 index.vue + components 子组件”的拆分模式，复杂页面（如 order, signing）进一步按功能域拆分 Tab 组件。
- 业务状态与枚举选项在组件内部通过常量数组（如 statusOptions）定义，并配合工具函数进行标签映射。
- 列表查询统一使用 `queryParams` 响应式对象管理筛选条件，并封装 `getList` 方法处理分页与加载状态。