- 采用 Dialog 弹窗模式处理新增与编辑业务，通过 `formType` 区分操作类型并复用同一表单组件。
- 使用 `v-hasPermi` 指令进行细粒度权限控制，确保按钮级操作符合后端定义的权限标识（如 `dealer:mgmt:create`）。
- 表单校验规则在 `DealerForm.vue` 内部通过 `reactive` 定义，重点约束业务关键字段（如名称、编码）的非空校验。