- API 层：`index.ts` 定义 RESTful 接口（分页、详情、新增、修改、删除）及精简列表查询，统一通过 `@/config/axios` 发起请求。
- 视图层：`index.vue` 负责列表展示与多条件筛选（名称、编码、状态），集成权限指令 `v-hasPermi` 控制操作按钮显隐。
- 表单层：`DealerForm.vue` 封装新增/编辑弹窗逻辑，通过 `defineExpose` 暴露 `open` 方法，实现父子组件通信与表单重置。
- 依赖方向：视图组件依赖 API 模块，API 模块依赖底层 Axios 配置，遵循单向数据流。