- **分层架构**：严格遵循 `api`（接口定义与类型）与 `views`（视图组件）分离的设计。
- **场景化组织**：按业务复杂度划分子目录，`demo01` 展示标准 CRUD，`demo02` 处理树形分类，`demo03` 细分为普通、内嵌及 ERP 风格的主子表实现。
- **组件化设计**：视图层采用“列表页 + 表单弹窗”模式，复杂场景（如 demo03）通过 `components` 目录拆解子表逻辑，利用 `defineExpose` 暴露校验与数据获取方法供父组件调用。