- API层：`api/infra/file/index.ts` 定义文件分页、删除及预签名获取接口；`api/infra/fileConfig/index.ts` 负责存储配置的CRUD及主配置切换。
- 视图层：`views/infra/file/` 实现文件列表展示与拖拽上传（集成 `useUpload` Hook）；`views/infra/fileConfig/` 提供基于存储器类型（Storage Type）动态渲染表单字段的配置编辑器。
- 交互逻辑：通过 `useUpload` 封装了“前端直连S3”与“后端代理上传”两种模式，根据环境变量自动切换上传策略。