- API 模块采用命名导出（Named Exports）组织 CRUD 方法，并在组件中通过 `import * as XxxApi` 方式引用以保持命名空间清晰。
- 弹窗表单组件统一暴露 `open` 方法供父组件调用，并通过 `defineEmits(['success'])` 在操作完成后触发父级列表刷新。
- 复杂表单（如 FileConfigForm）根据业务枚举值（如 storage 类型）使用 `v-if` 动态切换表单项，并维护统一的 `formRules` 校验对象。