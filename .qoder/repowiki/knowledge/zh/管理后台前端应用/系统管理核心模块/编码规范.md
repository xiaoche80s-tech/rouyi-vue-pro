- API 文件按业务域分包，统一导出 VO 接口定义与 CRUD 方法，路径遵循 `/system/{module}/index.ts`。
- 视图组件采用 `index.vue` 承载列表与搜索逻辑，`{Entity}Form.vue` 封装增改表单，通过 `ref` 触发弹窗。
- 列表页统一使用 `queryParams` 响应式对象管理筛选条件，并配套 `handleQuery` 与 `resetQuery` 标准处理方法。
- 所有涉及数据变更的操作（增删改、状态切换）均强制要求二次确认（`message.delConfirm` 或 `message.confirm`）。