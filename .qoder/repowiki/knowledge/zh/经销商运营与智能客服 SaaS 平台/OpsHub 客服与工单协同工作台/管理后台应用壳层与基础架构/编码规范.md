- 组件采用“目录+index导出”模式，即每个功能组件拥有独立目录，内部包含 `src/` 实现文件与 `index.ts` 统一导出入口。
- Store 模块同时导出带参的 `useXxxStore(store)` 和无参的 `useXxxStoreWithOut()`，以支持在组件外部（如路由守卫、工具类）直接调用。
- 路由守卫中采用“白名单+Token校验”机制，对 `/login` 等路径放行，其余路径强制校验 Access Token 并触发动态路由加载。