- 子组件通过 `defineExpose` 暴露 `settingValues` 方法，供父组件在“重新生成”场景下回填历史绘图参数。
- 各平台绘图组件均接收 `models` 属性，并在提交前校验当前选中模型在可用列表中的有效性，确保业务逻辑健壮性。
- 使用统一的 `AiPlatformEnum` 常量标识不同 AI 平台，并在 API 请求和组件切换逻辑中保持一致。
- 绘图任务提交前均执行二次确认（`message.confirm`），并对提示词长度、中文支持（SD）等进行前置校验。