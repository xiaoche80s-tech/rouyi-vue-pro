- **API 层**：`api/opshub/excelImport/index.ts` 封装了模板下载（单表/批量 ZIP）和文件上传接口。
- **视图层**：`views/opshub/excelImport/index.vue` 采用 L0-L5 依赖层级布局，将 16 张业务表划分为基础主数据、关联数据、业务主数据等分组。
- **组件层**：`ImportCard.vue` 作为通用卡片组件，处理单个业务表的枚举展示、模板下载与上传交互。
- **类型定义**：`types.ts` 统一定义了卡片配置项 `ImportCardItem` 和导入结果 `ImportResult` 结构。