- 业务列表页遵循“统计概览-条件筛选-数据表格-分页组件”的标准垂直布局结构。
- 跨模块业务咨询统一调用 `useCsConsult` Hook 并传入标准化的 `context` 与 `sourceModule` 参数。
- 复杂业务表单（如合同、工单处理）均采用独立 Modal 组件封装，并通过 `ref.open()` 模式触发。
- 基础维度数据（经销商、产品线）在各业务模块初始化时并行加载并缓存至本地 Ref。