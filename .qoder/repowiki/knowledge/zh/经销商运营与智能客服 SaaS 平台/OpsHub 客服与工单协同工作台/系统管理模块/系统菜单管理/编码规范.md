- API 模块采用命名导出函数封装 HTTP 请求，并统一导出 TypeScript 接口定义（如 `MenuVO`）。
- 表单弹窗组件通过 `defineExpose` 暴露 `open` 方法供父组件调用，而非使用 `v-model` 控制显隐。
- 列表页与表单页通过 `ref` 获取子组件实例并监听自定义 `success` 事件实现操作后的数据刷新。
- 字典数据通过 `getIntDictOptions` 配合 `DICT_TYPE` 枚举动态加载，用于下拉框、单选组等表单项。