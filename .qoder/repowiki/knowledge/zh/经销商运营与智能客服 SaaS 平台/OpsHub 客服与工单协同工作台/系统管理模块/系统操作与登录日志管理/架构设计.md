- 采用 API 层与视图层分离架构：`api/system/loginLog` 和 `api/system/operatelog` 封装分页查询与 Excel 导出接口，定义 TypeScript 数据契约。
- 视图层由列表页（`index.vue`）与详情页组件（`LoginLogDetail.vue` / `OperateLogDetail.vue`）组成，列表页负责搜索、分页与数据展示，详情页通过 `Dialog` 弹窗展示完整字段。
- 依赖全局工具库进行时间格式化、字典标签渲染及文件下载，并通过 `v-hasPermi` 指令实现细粒度的权限控制。