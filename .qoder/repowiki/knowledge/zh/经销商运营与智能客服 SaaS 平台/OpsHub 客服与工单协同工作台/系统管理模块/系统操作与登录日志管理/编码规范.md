- API 模块统一导出分页查询与导出方法，并定义对应的 VO 接口类型以规范前后端数据交互。
- 列表页均采用“搜索表单 + 数据表格 + 分页组件”的标准布局，支持按关键字、日期范围等多条件组合检索。
- 详情查看功能均通过独立的 Detail 组件实现，利用 `defineExpose` 暴露 `open` 方法供父组件调用弹窗。
- 导出操作均包含二次确认提示，并在请求过程中通过 `loading` 状态禁用按钮以防止重复提交。