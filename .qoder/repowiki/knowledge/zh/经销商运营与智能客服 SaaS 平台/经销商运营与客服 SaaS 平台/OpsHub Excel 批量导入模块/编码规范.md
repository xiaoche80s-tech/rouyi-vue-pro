- 导入方法统一返回 `ExcelImportRespVO`，包含成功数、插入/更新数及带有行号的失败原因映射（`failureRows`），便于前端精准定位错误。
- VO 类字段通过 `@ExcelProperty` 定义列名，枚举或字典字段额外标注 `@ExcelColumnSelect(functionName = "...")` 以在模板中生成动态下拉列表。
- Service 层导入逻辑普遍采用“先查后改”的幂等策略：根据唯一业务编码（如 dealerCode, orderCode）查询现有记录，存在则更新，不存在则插入，并实时维护内存缓存以支持后续行的关联校验。