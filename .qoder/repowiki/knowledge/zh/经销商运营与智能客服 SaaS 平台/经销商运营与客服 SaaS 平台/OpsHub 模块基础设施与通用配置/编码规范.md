- 数据权限规则采用集中式配置模式，在 Configuration 类中统一注册表名与字段映射，而非分散在各个 Mapper 或 Service 中。
- Excel 枚举转换遵循抽象基类模式，子类仅需实现 labelToCode 和 codeToLabel 两个 Map 即可完成双向转换逻辑。
- Excel 下拉数据源通过定义独立的 Bean 实现 ExcelColumnSelectFunction 接口，并以语义化的名称（如 common_status）注册到 Spring 容器。