- VO与DO分离：Controller层使用专门的Request/Response VO，通过`BeanUtils.toBean`与内部DO进行转换，避免领域模型直接暴露。
- 动态查询封装：在Mapper接口中使用默认方法配合`LambdaQueryWrapperX`的`eqIfPresent`、`likeIfPresent`等扩展方法，实现灵活的分页筛选逻辑。
- 业务状态计算下沉：将有效期状态（valid/expiring_soon/expired）的计算逻辑封装在Service层的静态方法`calculateExpireStatus`中，供Controller和Service复用。