- VO对象按功能子包隔离（如dict/vo/data, dict/vo/type），并严格区分PageReqVO、RespVO、SaveReqVO后缀。
- Service接口与实现类遵循命名规范，即XxxService接口对应XxxServiceImpl实现类。
- API接口定义在api包下，其实现类以ApiImpl结尾并标注@Service，内部委托给对应的Service处理。
- Controller层统一使用CommonResult包装返回值，并通过BeanUtils.toBean进行DO到VO的转换。