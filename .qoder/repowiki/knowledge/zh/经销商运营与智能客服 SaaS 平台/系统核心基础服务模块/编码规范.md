- API 接口层统一使用 `*Api` 命名，实现类使用 `*ApiImpl`，并放置在 `api` 包下供外部模块引用。
- Controller 层按管理端（`admin`）和客户端（`app`）物理隔离，分别处理后台管理与前端用户请求。
- 数据访问层（DAL）统一使用 `*DO` 表示数据库实体，`*Mapper` 表示数据访问接口，并遵循 `dal/dataobject` 与 `dal/mysql` 的分包规范。
- VO 对象在 Controller 层按操作类型细分（如 `req`/`resp`/`page`），避免直接暴露 DO 实体给前端。