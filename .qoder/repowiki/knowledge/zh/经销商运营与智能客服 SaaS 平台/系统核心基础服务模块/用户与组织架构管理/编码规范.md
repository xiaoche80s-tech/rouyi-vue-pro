- API 接口层统一返回 DTO 对象，并在 Interface 中提供默认的 `convertMap` 工具方法以简化批量查询后的数据处理。
- Controller 层通过 `@PreAuthorize` 注解配合权限表达式（如 `system:user:query`）实现细粒度的功能权限控制。
- 数据对象 DO 继承 `TenantBaseDO` 实现多租户隔离，并使用 `@TableField(typeHandler = JacksonTypeHandler.class)` 处理集合类型的字段存储。