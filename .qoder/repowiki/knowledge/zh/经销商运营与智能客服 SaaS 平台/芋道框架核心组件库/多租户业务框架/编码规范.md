- 使用 TransmittableThreadLocal 承载租户上下文，确保线程池/异步场景下的租户ID不丢失。
- 通过 @TenantIgnore 注解统一控制 Web/DB/AOP 层面的租户忽略行为，并在过滤器与拦截器中保持一致的忽略语义。
- 在 Filter/Interceptor/Aspect 的执行链路中采用 try-finally 清理或恢复 TenantContextHolder，避免上下文泄漏。
- 对定时任务采用按租户并行执行的模型，并通过工具方法在执行前切换租户上下文，保证业务逻辑无需显式处理租户。