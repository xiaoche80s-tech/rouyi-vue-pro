基于 Spring Boot AutoConfiguration 与 Spring Data Redis（Pub/Sub + Stream）；RabbitMQ 使用 `spring-rabbit` 并以 `Jackson2JsonMessageConverter` 作为消息转换器；可选依赖 `spring-kafka`、`rocketmq-spring-boot-starter`（当前未提供对应自动配置）。