- 缓存名称支持通过 '#' 后缀定义过期时间（如 'user#300s'），由 `TimeoutRedisCacheManager` 解析并覆盖默认配置。
- 所有 Redis Value 序列化均通过 `buildRedisSerializer()` 统一处理，强制注册 `JavaTimeModule` 以支持 Java 8 时间类型。
- 自动配置类使用 `@AutoConfiguration` 注解，并通过 `before` 属性控制加载顺序，确保自定义 Bean 覆盖默认实现。