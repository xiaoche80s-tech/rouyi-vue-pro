- 使用 @ConditionalOnClass 和 @ConditionalOnProperty 确保监控组件在缺少对应依赖或显式关闭时不初始化，提高 Starter 的兼容性。
- 通过 AOP 切面处理业务追踪逻辑，利用 SpEL 表达式解析注解中的业务属性并自动打标到 Tracing Span 中。
- 遵循 WebFilterOrderEnum 定义的顺序注册过滤器，确保 TraceFilter 在链路早期执行并能正确向客户端返回 trace-id。