- 分层结构：`config`包提供测试环境配置（内嵌Redis Server、SQL初始化），`core.ut`包定义四种测试基类（纯Mockito、DB、Redis、DB+Redis），`core.util`包提供断言增强与POJO随机生成工具。
- 依赖注入：通过`@SpringBootTest`结合`@Import`显式加载MyBatis、Druid、Redis等自动配置类，实现轻量级容器启动。
- 技术选型：使用H2作为内存数据库，jedis-mock模拟Redis服务，Podam库负责复杂POJO对象的自动化随机填充。