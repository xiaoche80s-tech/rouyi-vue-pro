- **JSON 处理**：基于 Jackson 定制 `ObjectMapper`，支持 LocalDateTime 时间戳序列化（见 `JsonUtils`）。
- **校验增强**：集成 Jakarta Validation 并扩展 `@InEnum`、`@Mobile` 等自定义注解。
- **辅助库**：深度集成 Hutool (`hutool-all`) 进行基础工具封装，使用 MapStruct 进行对象转换。