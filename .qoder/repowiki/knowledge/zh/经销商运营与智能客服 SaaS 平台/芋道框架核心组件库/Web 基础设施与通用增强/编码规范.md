- 所有自动配置类遵循 `Yudao[Module]AutoConfiguration` 命名规范，并标注 `@AutoConfiguration` 与 `@EnableConfigurationProperties`。
- 过滤器注册统一使用 `YudaoWebAutoConfiguration.createFilterBean` 工具方法，确保执行顺序通过 `WebFilterOrderEnum` 集中管理。
- 敏感数据处理采用注解驱动模式，定义专用注解（如 `@EmailDesensitize`）配合 `DesensitizationHandler` 实现业务逻辑与框架解耦。