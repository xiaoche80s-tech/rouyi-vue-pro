- 所有 WebSocket 消息均采用统一的 JSON 结构（包含 type 和 content 字段），通过 `JsonUtils` 进行序列化与反序列化。
- 消息发送接口 `WebSocketMessageSender` 提供针对特定用户、用户类型或 Session ID 的重载方法，并默认支持对象到 JSON 字符串的转换。
- 握手拦截器与消息处理器均从 `WebSocketSession` 的属性中提取租户与用户信息，以确保多租户环境下的数据隔离。