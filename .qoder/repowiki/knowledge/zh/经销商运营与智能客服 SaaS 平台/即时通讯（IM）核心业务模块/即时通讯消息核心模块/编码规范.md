- 控制器方法统一通过 `SecurityFrameworkUtils.getLoginUserId()` 获取当前操作用户 ID，并作为参数传递给 Service 层，确保身份上下文的一致性。
- Service 接口明确区分【用户调用】与【系统调用】入口，例如 `sendPrivateMessage` 提供基于 VO 的业务校验版本和基于 DTO 的内部透传版本，以适配不同场景的幂等与校验需求。
- 消息内容中的引用（Quote）逻辑在 Service 层进行统一规范化处理，包括校验原消息有效性、构建标准化的 `QuoteMessage` 对象并注入内容 JSON，确保前端渲染一致性。