- 控制器方法统一返回 `CommonResult` 包装类，成功结果通过 `CommonResult.success()` 构建。
- 服务层接口与实现类分离，接口定义业务契约，实现类处理具体逻辑及数据转换（如 `BeanUtils.toBean`）。
- 管理后台接口普遍使用 `@PreAuthorize` 注解进行权限控制，权限标识遵循 `im:manager:xxx:action` 格式。
- 数据对象（DO）继承 `BaseDO` 以获取通用审计字段（如创建时间、更新人等），并使用 Lombok 简化代码。