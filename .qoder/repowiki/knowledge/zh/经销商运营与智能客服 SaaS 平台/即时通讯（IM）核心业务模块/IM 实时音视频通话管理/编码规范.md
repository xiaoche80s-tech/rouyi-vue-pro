- 状态机驱动开发：通话生命周期严格遵循 CREATED → RUNNING → ENDED 状态流转，所有业务操作均基于当前状态进行校验与转换。
- 条件更新保障幂等：在参与者状态变更（如接听、离开）及通话结束场景中，广泛使用带状态前置条件的 SQL 更新，防止并发请求导致的状态错乱。
- 信令与消息流分离：核心信令（如 INVITE, REJECT）通过 WebSocket 实时推送，而通话开始/结束等关键节点则同步写入 IM 消息流作为历史凭证。