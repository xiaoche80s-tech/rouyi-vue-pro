- 采用嵌套静态内部类的方式组织 `ImProperties` 配置项，按业务子域（Friend, Group, Rtc 等）划分，保持 YAML 配置路径的语义清晰。
- 所有业务错误码统一定义在 `ErrorCodeConstants` 接口中，并按模块段（如 1-040-300-000 为消息）进行数值区间划分，便于问题定位。
- RTC 相关的身份标识（Identity）采用纯数字 UserId 或预留的 `UserId#Terminal` 格式，并提供统一的 `buildIdentity` 和 `parseUserId` 工具方法进行转换。