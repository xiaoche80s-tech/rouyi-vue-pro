- 在组装包含用户信息的 VO 列表时，统一采用批量查询用户信息（`adminUserApi.getUserMap`）并内存映射的方式，避免 N+1 查询性能问题。
- 涉及双边关系状态变更的操作（如添加、删除、拉黑），均使用 `@Caching` 注解同时清除 A->B 和 B->A 两个方向的 `FRIEND_STATE` 缓存，确保状态一致性。
- 好友关系的删除采用单向软删除策略（更新 `status` 为 DISABLE 并记录 `deleteTime`），保留对端视角的关系完整性，符合 IM 单边删除的业务语义。