- 所有业务模块遵循统一的包结构规范：controller/admin, service, dal (data access layer), convert, enums。
- 跨模块调用优先通过 API 层（如 `bpm/api`）或事件机制解耦，避免直接依赖内部 Service 实现。
- 持久层统一使用 MyBatis-Plus，并通过 `yudao-spring-boot-starter-mybatis` 封装通用的 BaseMapper 和分页逻辑。