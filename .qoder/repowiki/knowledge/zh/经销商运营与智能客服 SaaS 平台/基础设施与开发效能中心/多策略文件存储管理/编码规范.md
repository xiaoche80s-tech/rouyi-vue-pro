- 所有文件路径在存入存储器前必须经过 `FilePathUtils.validatePath` 校验，防止目录穿越攻击。
- 文件上传时若未提供文件名，默认使用 SHA256 哈希值作为文件名以确保唯一性和安全性。
- 存储客户端的预签名操作（presignPutUrl/presignGetUrl）在不支持的实现中默认抛出 `UnsupportedOperationException`。
- 文件删除操作采用双重校验：先校验数据库记录存在性，再校验路径合法性，最后才调用客户端执行物理删除。