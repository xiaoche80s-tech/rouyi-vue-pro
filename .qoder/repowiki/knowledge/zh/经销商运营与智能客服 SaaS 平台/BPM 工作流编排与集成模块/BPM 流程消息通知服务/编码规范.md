- 所有请求 DTO 均放置在 `dto` 子包下，并采用 `BpmMessageSendWhen[Scenario]ReqDTO` 的命名规范以明确触发场景。
- DTO 字段校验统一使用 Jakarta Validation 注解（如 `@NotEmpty`, `@NotNull`），并在 Service 接口参数处通过 `@Valid` 触发校验。
- 短信模板参数统一使用 `Map<String, Object>` 承载，键名采用驼峰命名法对应模板中的占位符。