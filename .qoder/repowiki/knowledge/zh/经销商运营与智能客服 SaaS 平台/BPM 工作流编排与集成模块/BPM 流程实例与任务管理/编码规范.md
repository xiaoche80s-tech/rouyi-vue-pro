- Controller 层统一使用 `CommonResult` 包装返回结果，并通过 `@PreAuthorize` 注解进行细粒度的权限控制。
- 分页查询采用“先查引擎数据，后补全业务属性”的模式，利用 `convertMap` 批量获取用户、部门及流程定义信息以减少 RPC 调用次数。
- Service 接口明确区分 Query（查询）、Update（写入）和 Event（事件）三类方法，保持接口语义清晰。
- VO 设计遵循场景隔离原则，针对实例、任务、抄送等不同业务场景定义独立的 ReqVO 和 RespVO。