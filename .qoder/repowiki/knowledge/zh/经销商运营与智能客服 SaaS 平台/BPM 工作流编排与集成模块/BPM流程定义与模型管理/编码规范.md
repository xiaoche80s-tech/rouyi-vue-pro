- 控制器方法统一返回`CommonResult<T>`包装类，并使用Swagger注解（@Operation, @Parameter）完善API文档。
- Service接口与实现类严格分离，命名遵循`XxxService`与`XxxServiceImpl`规范，且接口中提供常用的Map转换默认方法。
- VO对象按功能模块分包（如model, form, category），并细分为PageReqVO（分页查询）、RespVO（响应）、SaveReqVO（新增/修改）三类。