- API 事件对象（Event POJO）置于 api/event 包以保持零框架依赖，而依赖 Flowable 核心类的监听器实现置于 framework/flowable 包。
- 所有 Service 实现类遵循 *Service/*ServiceImpl 命名规范，并通过 @Lazy 注解解决流程引擎监听器与业务 Service 之间的循环依赖。
- 控制器层按功能域划分为 admin/definition（流程定义）、admin/task（任务管理）及 admin/oa（示例业务），并统一使用 VO 对象进行前后端数据传输。