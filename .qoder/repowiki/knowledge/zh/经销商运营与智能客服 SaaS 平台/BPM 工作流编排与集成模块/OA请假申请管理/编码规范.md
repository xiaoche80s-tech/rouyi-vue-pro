- 控制器方法统一返回`CommonResult`包装类，并使用`@PreAuthorize`注解配合SpEL表达式进行细粒度权限控制。
- 服务实现类中使用`@Transactional`注解确保创建请假单与发起流程实例的数据一致性，并在异常时回滚。
- VO类中使用Jakarta Validation注解（如`@NotNull`, `@AssertTrue`）进行参数校验，确保业务规则在入口层被验证。